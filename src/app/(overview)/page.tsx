import Card from "../ui/ency/card";
// import { fetchAll, fetchProp } from "../lib/data";

export default function Home() {
  // let list = fetchAll()
  return (
    <main className="flex min-h-screen min-w-screen flex-col items-center justify-between p-24">
        {/* {
          list.map((name) => (
            <div key={name}>
                <p>{name}</p>
                <p>{fetchProp(name, 'ZukanIndex')}</p>
            </div>
          ))
        } */}
    </main>
  );
}
